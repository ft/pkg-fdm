Using -iquote. instead of -I- for gmake.

This surpresses warnings in newer versions of gcc.
The patch is purely cosmetical.

Index: fdm-1.6/GNUmakefile
===================================================================
--- fdm-1.6.orig/GNUmakefile	2009-04-14 06:00:51.000000000 +0200
+++ fdm-1.6/GNUmakefile	2009-04-14 06:00:58.000000000 +0200
@@ -72,7 +72,7 @@
 endif
 
 OBJS= $(patsubst %.c,%.o,$(SRCS))
-CPPFLAGS+= $(DEFS) -I. -I- $(INCDIRS)
+CPPFLAGS+= $(DEFS) -I. -iquote. $(INCDIRS)
 ifdef DEBUG
 CFLAGS+= -g -ggdb -DDEBUG
 LDFLAGS+= -rdynamic
